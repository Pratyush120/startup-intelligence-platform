# =============================================================================
# requirements/base.txt
#
# Direct, top-level runtime dependencies only.
# Every package here is confirmed:
#   - Actually imported in src/ (verified by source audit 2026-07-19)
#   - Compatible with Python 3.11 (cp311 wheels confirmed on PyPI)
#   - Pinned with ~= (compatible-release) to allow patch updates
#     while preventing breaking major/minor changes
#
# Pinning policy:
# =============================================================================

fastapi~=0.115.12
uvicorn[standard]~=0.34.3        # [standard] includes websockets + h11 extras

pydantic~=2.11.7
pydantic-settings~=2.10.1

structlog~=25.4.0

# pandas is used in src/services/data_service.py for DataFrame returns.
pandas~=2.3.0

# numpy: 2.5.x requires Python >=3.12; 2.2.x is the latest Python 3.11 branch.
# cp311 wheel confirmed on PyPI for numpy==2.2.6.
numpy~=2.2.6

# Used in src/collectors/base_collector.py + urllib3 retry adapter
requests~=2.32.3

# Used in src/collectors/google_news_collector.py
feedparser~=6.0.11

# Used by pydantic-settings for .env file loading
python-dotenv~=1.1.0

# Used in src/pipeline/deduplicator.py and src/api/search.py
rapidfuzz~=3.13.0

# Used in src/pipeline/llm_analyzer.py (lazy import guarded by try/except)
# Pinned to 1.x to avoid 2.x breaking API surface changes
openai~=1.93.3

alembic~=1.14.0

celery~=5.4.0
redis~=5.2.1
duckduckgo-search~=6.2.0
