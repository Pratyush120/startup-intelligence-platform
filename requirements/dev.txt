# =============================================================================
# requirements/dev.txt
#
# Development + testing dependencies only.
# Never installed in Docker/Render production builds.
#
# Usage:
#   pip install -r requirements/dev.txt
# =============================================================================

-r base.txt

# ── Testing ───────────────────────────────────────────────────────────────────
pytest~=8.4.0
pytest-asyncio~=0.26.0
pytest-cov~=6.2.0
httpx~=0.28.1                    # ASGI test client for FastAPI

# ── Code Quality ─────────────────────────────────────────────────────────────
ruff~=0.12.3                     # Fast linter + formatter (replaces flake8+black)
mypy~=1.16.0                     # Static type checking

# ── Dependency Management ─────────────────────────────────────────────────────
pip-tools~=7.4.1                 # Compile + lock dependency trees

# ── Debugging ────────────────────────────────────────────────────────────────
ipython~=8.37.0
